[
  {
    "name": "generateReports",
    "body": "() => {\n  const lines = getBulkInvoiceLines.data || [];\n  if (lines.length === 0) {\n    showAlert(\"No data found for this scheme in selection.\", \"warning\");\n    return;\n  }\n  const prodData = Scheme_Reports.getProductSummary(lines);\n  const custData = Scheme_Reports.getCustomerSummary(lines);\n  const dseData = Scheme_Reports.getDSESummary(lines);\n  const tierData = Scheme_Reports.getTierSummary(lines);\n  const auditData = Scheme_Reports.getInvoiceSummary(lines);\n  storeValue('report_products', prodData);\n  storeValue('report_customers', custData);\n  storeValue('report_dse', dseData);\n  storeValue('report_tiers', tierData);\n  storeValue('report_audit', auditData);\n  showAlert(`Generated 5 Scheme-Focused reports!`, \"success\");\n}"
  },
  {
    "name": "generateReports",
    "body": "() => {\n  const lines = getBulkInvoiceLines.data || [];\n  if (lines.length === 0) {\n    showAlert(\"No data found for this scheme in selection.\", \"warning\");\n    return;\n  }\n  const prodData = Scheme_Reports.getProductSummary(lines);\n  const custData = Scheme_Reports.getCustomerSummary(lines);\n  const dseData = Scheme_Reports.getDSESummary(lines);\n  const tierData = Scheme_Reports.getTierSummary(lines);\n  const auditData = Scheme_Reports.getInvoiceSummary(lines);\n  storeValue('report_products', prodData);\n  storeValue('report_customers', custData);\n  storeValue('report_dse', dseData);\n  storeValue('report_tiers', tierData);\n  storeValue('report_audit', auditData);\n  showAlert(`Generated 5 Scheme-Focused reports!`, \"success\");\n}"
  },
  {
    "name": "AttendanceReport",
    "method": "GET",
    "url": "/api/employees/attendance/details",
    "body": ""
  },
  {
    "name": "saleReport",
    "method": "GET",
    "url": "/api/analytics/sales-fy-report",
    "body": ""
  },
  {
    "name": "previewStockReport",
    "body": "async stockData => {\n  try {\n    if (!stockData || stockData.length === 0) throw new Error(\"No stock data available to export.\");\n    if (typeof jspdf === \"undefined\") throw new Error(\"jspdf library not loaded.\");\n    const jsPDFConstructor = jspdf.jsPDF || jspdf;\n    const doc = new jsPDFConstructor('l', 'pt', 'a4');\n    if (typeof doc.autoTable !== \"function\" && jspdf.plugin && jspdf.plugin.autotable) {\n      jsPDFConstructor.API.autoTable = jspdf.plugin.autotable;\n    }\n    const brand = Global_Assets.getSummary();\n    const margin = 15;\n    const pageWidth = doc.internal.pageSize.width;\n    const drawMainHeader = (currentPage, totalPages) => {\n      const headerY = margin;\n      try {\n        const logo = Global_Assets.getLogo();\n        if (logo.startsWith(\"data:image/\")) {\n          doc.addImage(logo, 'PNG', margin, headerY, 80, 25);\n        }\n      } catch (e) {}\n      doc.setTextColor(0, 0, 0);\n      doc.setFont(\"helvetica\", \"bold\");\n      doc.setFontSize(14);\n      doc.text(\"INVENTORY STATUS & VALUATION REPORT\", pageWidth / 2, headerY + 12, {\n        align: \"center\"\n      });\n      doc.setFontSize(9);\n      doc.setFont(\"helvetica\", \"normal\");\n      doc.text(`${brand.regt_name} | GST: ${brand.gst}`, pageWidth / 2, headerY + 25, {\n        align: \"center\"\n      });\n      const boxY = headerY + 40;\n      const boxWidth = (pageWidth - margin * 2 - 10) / 2;\n      doc.rect(margin, boxY, boxWidth, 40);\n      doc.setFont(\"helvetica\", \"bold\");\n      doc.text(\"Report Date:\", margin + 5, boxY + 15);\n      doc.text(\"Generated By:\", margin + 5, boxY + 30);\n      doc.setFont(\"helvetica\", \"normal\");\n      doc.text(moment().format(\"DD MMM YYYY, hh:mm A\"), margin + 70, boxY + 15);\n      doc.text(\"System Generated\", margin + 70, boxY + 30);\n      doc.rect(margin + boxWidth + 10, boxY, boxWidth, 40);\n      doc.setFont(\"helvetica\", \"bold\");\n      doc.text(\"Page:\", margin + boxWidth + 15, boxY + 15);\n      doc.text(\"Status:\", margin + boxWidth + 15, boxY + 30);\n      doc.setFont(\"helvetica\", \"normal\");\n      doc.text(`${currentPage} of ${totalPages}`, margin + boxWidth + 60, boxY + 15);\n      doc.text(\"Live Inventory Data\", margin + boxWidth + 60, boxY + 30);\n      return boxY + 40;\n    };\n    doc.autoTable({\n      startY: 100,\n      margin: {\n        left: margin,\n        right: margin,\n        top: 100\n      },\n      head: [[\"S.N\", \"ITEM NAME\", \"CODE\", \"BRAND\", \"CATEGORY\", \"MRP\", \"QTY\", \"UOM\", \"COST\", \"TOTAL VALUE\", \"LAST SOLD\"]],\n      body: stockData.map((row, index) => [index + 1, row.product_name, row.product_code, row.brand_name, row.category_name, Number(row.mrp).toFixed(2), row.current_stock, row.uom, Number(row.purchase_rate).toFixed(2), (Number(row.current_stock) * Number(row.purchase_rate)).toFixed(2), row.last_sold_date ? moment(row.last_sold_date).format(\"DD/MM/YY\") : \"N/A\"]),\n      didDrawPage: data => {\n        drawMainHeader(data.pageNumber, doc.internal.getNumberOfPages());\n      },\n      theme: 'grid',\n      styles: {\n        fontSize: 8,\n        cellPadding: 3,\n        textColor: [0, 0, 0],\n        lineColor: [200, 200, 200]\n      },\n      headStyles: {\n        fillColor: [240, 240, 240],\n        textColor: [0, 0, 0],\n        fontStyle: 'bold'\n      },\n      columnStyles: {\n        0: {\n          cellWidth: 25\n        },\n        1: {\n          cellWidth: 'auto'\n        },\n        6: {\n          halign: 'right',\n          fontStyle: 'bold'\n        },\n        9: {\n          halign: 'right',\n          fontStyle: 'bold'\n        }\n      }\n    });\n    const finalY = doc.lastAutoTable.finalY + 20;\n    const totalVal = stockData.reduce((sum, r) => sum + Number(r.current_stock) * Number(r.purchase_rate), 0);\n    const totalQty = stockData.reduce((sum, r) => sum + Number(r.current_stock), 0);\n    doc.setFont(\"helvetica\", \"bold\");\n    doc.setFontSize(10);\n    doc.text(`Total Items in Stock: ${totalQty}`, margin, finalY);\n    doc.text(`Total Inventory Valuation: INR ${totalVal.toLocaleString('en-IN', {\n      minimumFractionDigits: 2\n    })}`, margin, finalY + 15);\n    const fileName = `Stock_Report_${moment().format(\"DD_MM_YY\")}.pdf`;\n    download(doc.output('dataurlstring'), fileName, \"application/pdf\");\n    showAlert(\"Stock Report Exported Successfully\", \"success\");\n  } catch (error) {\n    showAlert(\"Export Error: \" + error.message, \"error\");\n  }\n}"
  },
  {
    "name": "exportForensicReport",
    "body": "async function (forensicData) {\n  try {\n    if (!forensicData) throw new Error(\"No forensic snapshot data provided.\");\n    if (typeof jspdf === \"undefined\") throw new Error(\"jspdf library not loaded.\");\n    const jsPDFConstructor = jspdf.jsPDF || jspdf;\n    const doc = new jsPDFConstructor('p', 'pt', 'a4');\n    if (typeof doc.autoTable !== \"function\" && jspdf.plugin && jspdf.plugin.autotable) {\n      jsPDFConstructor.API.autoTable = jspdf.plugin.autotable;\n    }\n    const brand = Global_Assets.getSummary();\n    const margin = 20;\n    const pageWidth = doc.internal.pageSize.width;\n    const pageHeight = doc.internal.pageSize.height;\n    const drawHeader = (currentPage, totalPages) => {\n      const headerY = margin + 5;\n      try {\n        const logo = Global_Assets.getLogo();\n        if (logo && logo.startsWith(\"data:image/\")) {\n          doc.addImage(logo, 'PNG', margin, headerY, 90, 25);\n        }\n      } catch (e) {}\n      doc.setTextColor(30, 41, 59);\n      doc.setFont(\"helvetica\", \"bold\");\n      doc.setFontSize(14);\n      doc.text(\"FORENSIC FINANCIAL SNAPSHOT\", pageWidth - margin, headerY + 10, {\n        align: \"right\"\n      });\n      doc.setFontSize(8.5);\n      doc.setFont(\"helvetica\", \"normal\");\n      doc.setTextColor(100, 116, 139);\n      const generatedDate = moment(forensicData.timestamp).format(\"DD MMM YYYY, hh:mm A\");\n      doc.text(`Snapshot Generated: ${generatedDate}`, pageWidth - margin, headerY + 22, {\n        align: \"right\"\n      });\n      doc.setDrawColor(226, 232, 240);\n      doc.line(margin, headerY + 32, pageWidth - margin, headerY + 32);\n      const boxesY = headerY + 40;\n      const gap = 10;\n      const boxWidth = (pageWidth - margin * 2 - gap * 2) / 3;\n      const boxHeight = 50;\n      JSObject1._drawStatBox(doc, margin, boxesY, boxWidth, boxHeight, \"TOTAL REAL ASSETS\", `INR ${Number(forensicData.summary.total_assets).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })}`);\n      JSObject1._drawStatBox(doc, margin + boxWidth + gap, boxesY, boxWidth, boxHeight, \"TOTAL LIABILITIES\", `INR ${Number(forensicData.summary.total_liabilities).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })}`);\n      const netCapital = Number(forensicData.summary.net_capital);\n      JSObject1._drawStatBox(doc, margin + boxWidth * 2 + gap * 2, boxesY, boxWidth, boxHeight, \"NET CAPITAL (EQUITY)\", `INR ${netCapital.toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })}`, netCapital < 0);\n      return boxesY + boxHeight + 15;\n    };\n    const liquidRows = (forensicData.accounts || []).map(acc => [acc.name, acc.category, Number(acc.balance).toLocaleString('en-IN', {\n      minimumFractionDigits: 2\n    })]);\n    const partyRows = [];\n    (forensicData.receivables || []).slice(0, 5).forEach(r => {\n      partyRows.push([r.party, \"Receivable (Customer)\", `${r.pending_bills} Bills`, Number(r.balance).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    (forensicData.payables || []).slice(0, 5).forEach(p => {\n      partyRows.push([p.party, \"Payable (Vendor)\", \"-\", Number(p.balance).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    const loanRows = [];\n    (forensicData.loans_given || []).forEach(l => {\n      loanRows.push([l.party_name, \"LOAN GIVEN (Asset)\", l.status, Number(l.balance).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    (forensicData.loans_taken || []).forEach(l => {\n      loanRows.push([l.party_name, \"LOAN TAKEN (Liability)\", l.status, Number(l.balance).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    const assetRows = [];\n    (forensicData.stock || []).slice(0, 5).forEach(s => {\n      assetRows.push([s.product_name, \"Inventory (Stock)\", `${Number(s.stock_qty).toLocaleString()} Units`, Number(s.valuation).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    (forensicData.assets_list || []).forEach(a => {\n      assetRows.push([a.asset_name, `Fixed Asset (${a.category})`, a.status, Number(a.purchase_cost).toLocaleString('en-IN', {\n        minimumFractionDigits: 2\n      })]);\n    });\n    const chequeRows = (forensicData.cheques_list || []).map(c => [c.cheque_number, moment(c.cheque_date).format(\"DD/MM/YYYY\"), c.party_name, c.bank_name, Number(c.amount).toLocaleString('en-IN', {\n      minimumFractionDigits: 2\n    })]);\n    let startY = 125;\n    doc.setFont(\"helvetica\", \"bold\");\n    doc.setFontSize(10);\n    doc.setTextColor(51, 65, 85);\n    doc.text(\"1. LIQUID ACCOUNTS & BALANCES\", margin, startY);\n    doc.autoTable({\n      startY: startY + 5,\n      margin: {\n        left: margin,\n        right: margin,\n        bottom: 60\n      },\n      head: [[\"ACCOUNT NAME\", \"CATEGORY\", \"BALANCE (INR)\"]],\n      body: liquidRows,\n      theme: 'striped',\n      styles: {\n        fontSize: 8,\n        cellPadding: 4.5,\n        textColor: [30, 41, 59]\n      },\n      headStyles: {\n        fillColor: [51, 65, 85],\n        fontStyle: 'bold'\n      },\n      columnStyles: {\n        2: {\n          halign: 'right',\n          fontStyle: 'bold'\n        }\n      },\n      didDrawPage: data => {\n        drawHeader(data.pageNumber, doc.internal.getNumberOfPages());\n      }\n    });\n    startY = doc.lastAutoTable.finalY + 20;\n    if (startY > pageHeight - 120) {\n      doc.addPage();\n      startY = 125;\n    }\n    doc.setFont(\"helvetica\", \"bold\");\n    doc.setFontSize(10);\n    doc.setTextColor(51, 65, 85);\n    doc.text(\"2. OUTSTANDING RECEIVABLES & PAYABLES (TOP PARTIES)\", margin, startY);\n    doc.autoTable({\n      startY: startY + 5,\n      margin: {\n        left: margin,\n        right: margin,\n        bottom: 60\n      },\n      head: [[\"PARTY NAME\", \"CLASSIFICATION\", \"DETAILS\", \"BALANCE (INR)\"]],\n      body: partyRows.length > 0 ? partyRows : [[\"No outstanding parties\", \"-\", \"-\", \"0.00\"]],\n      theme: 'striped',\n      styles: {\n        fontSize: 8,\n        cellPadding: 4.5,\n        textColor: [30, 41, 59]\n      },\n      headStyles: {\n        fillColor: [71, 85, 105],\n        fontStyle: 'bold'\n      },\n      columnStyles: {\n        3: {\n          halign: 'right',\n          fontStyle: 'bold'\n        }\n      }\n    });\n    startY = doc.lastAutoTable.finalY + 20;\n    if (startY > pageHeight - 120) {\n      doc.addPage();\n      startY = 125;\n    }\n    doc.setFont(\"helvetica\", \"bold\");\n    doc.setFontSize(10);\n    doc.setTextColor(51, 65, 85);\n    doc.text(\"3. OUTSTANDING LOANS\", margin, startY);\n    doc.autoTable({\n      startY: startY + 5,\n      margin: {\n        left: margin,\n        right: margin,\n        bottom: 60\n      },\n      head: [[\"PARTY NAME\", \"LOAN TYPE / DIRECTION\", \"STATUS\", \"PRINCIPAL BALANCE (INR)\"]],\n      body: loanRows.length > 0 ? loanRows : [[\"No outstanding loans\", \"-\", \"-\", \"0.00\"]],\n      theme: 'striped',\n      styles: {\n        fontSize: 8,\n        cellPadding: 4.5,\n        textColor: [30, 41, 59]\n      },\n      headStyles: {\n        fillColor: [51, 65, 85],\n        fontStyle: 'bold'\n      },\n      columnStyles: {\n        3: {\n          halign: 'right',\n          fontStyle: 'bold'\n        }\n      }\n    });\n    startY = doc.lastAutoTable.finalY + 20;\n    if (startY > pageHeight - 120) {\n      doc.addPage();\n      startY = 125;\n    }\n    doc.setFont(\"helvetica\", \"bold\");\n    doc.setFontSize(10);\n    doc.setTextColor(51, 65, 85);\n    doc.text(\"4. INVENTORY VALUATIONS & FIXED ASSETS\", margin, startY);\n    doc.autoTable({\n      startY: startY + 5,\n      margin: {\n        left: margin,\n        right: margin,\n        bottom: 60\n      },\n      head: [[\"ITEM / ASSET NAME\", \"CLASSIFICATION\", \"QUANTITY / STATUS\", \"VALUATION / COST (INR)\"]],\n      body: assetRows.length > 0 ? assetRows : [[\"No active valuations reported\", \"-\", \"-\", \"0.00\"]],\n      theme: 'striped',\n      styles: {\n        fontSize: 8,\n        cellPadding: 4.5,\n        textColor: [30, 41, 59]\n      },\n      headStyles: {\n        fillColor: [71, 85, 105],\n        fontStyle: 'bold'\n      },\n      columnStyles: {\n        3: {\n          halign: 'right',\n          fontStyle: 'bold'\n        }\n      }\n    });\n    if (chequeRows.length > 0) {\n      startY = doc.lastAutoTable.finalY + 20;\n      if (startY > pageHeight - 120) {\n        doc.addPage();\n        startY = 125;\n      }\n      doc.setFont(\"helvetica\", \"bold\");\n      doc.setFontSize(10);\n      doc.setTextColor(51, 65, 85);\n      doc.text(\"5. PENDING INCOMING CHEQUES\", margin, startY);\n      doc.autoTable({\n        startY: startY + 5,\n        margin: {\n          left: margin,\n          right: margin,\n          bottom: 60\n        },\n        head: [[\"CHEQUE NO\", \"CHEQUE DATE\", \"PARTY NAME\", \"BANK NAME\", \"AMOUNT (INR)\"]],\n        body: chequeRows,\n        theme: 'striped',\n        styles: {\n          fontSize: 8,\n          cellPadding: 4.5,\n          textColor: [30, 41, 59]\n        },\n        headStyles: {\n          fillColor: [51, 65, 85],\n          fontStyle: 'bold'\n        },\n        columnStyles: {\n          4: {\n            halign: 'right',\n            fontStyle: 'bold'\n          }\n        }\n      });\n    }\n    const totalPages = doc.internal.getNumberOfPages();\n    doc.setFontSize(7.5);\n    doc.setFont(\"helvetica\", \"normal\");\n    doc.setTextColor(148, 163, 184);\n    for (let i = 1; i <= totalPages; i++) {\n      doc.setPage(i);\n      doc.text(`${brand.regt_name} | GST: ${brand.gst} | Email: ${brand.email}`, pageWidth / 2, pageHeight - 25, {\n        align: \"center\"\n      });\n      doc.text(`Address: ${brand.address}, ${brand.District} - ${brand.pin} | Page ${i} of ${totalPages}`, pageWidth / 2, pageHeight - 15, {\n        align: \"center\"\n      });\n    }\n    const fileName = `Forensic_Report_${moment(forensicData.timestamp).format(\"YYYYMMDD_HHmm\")}.pdf`;\n    download(doc.output('dataurlstring'), fileName, \"application/pdf\");\n    showAlert(\"Forensic Financial Snapshot PDF Generated\", \"success\");\n  } catch (error) {\n    showAlert(\"Forensic PDF Error: \" + error.message, \"error\");\n  }\n}"
  },
  {
    "name": "getPendingReports",
    "method": "GET",
    "url": "/api/finance/reconciliation/list",
    "body": ""
  },
  {
    "name": "getReportDetails",
    "method": "GET",
    "url": "/api/finance/reconciliation/{{ tblPendingDSR.triggeredRow.report_id }}/details",
    "body": ""
  },
  {
    "name": "downloadTripReports",
    "body": "async type => {\n  if (type === \"PICKLIST\") {\n    await getPickList.run();\n    if (!getPickList.data || !getPickList.data.trip_info) {\n      return showAlert(\"Failed to generate Picklist. Check API.\", \"error\");\n    }\n    const tripInfo = {\n      trip_number: getPickList.data.trip_info.trip_number || \"-\",\n      date: getPickList.data.trip_info.date || new Date(),\n      vehicle_number: getPickList.data.trip_info.vehicle_number || \"Not Assigned\",\n      driver_name: getPickList.data.trip_info.driver_name || \"Unassigned\",\n      team: getPickList.data.trip_info.team_name || \"General Team\"\n    };\n    TripReports.generatePickList(getPickList.data.items || [], tripInfo);\n  } else if (type === \"DELIVERY_LIST\") {\n    await getDeliveryList.run();\n    if (!getDeliveryList.data || !getDeliveryList.data.trip_info) {\n      return showAlert(\"Failed to generate Delivery List. Check API.\", \"error\");\n    }\n    const tripInfo = {\n      trip_number: getDeliveryList.data.trip_info.trip_number || \"-\",\n      date: getDeliveryList.data.trip_info.date || new Date(),\n      vehicle_number: getDeliveryList.data.trip_info.vehicle_number || \"Not Assigned\",\n      driver_name: getDeliveryList.data.trip_info.driver_name || \"Unassigned\",\n      team: getDeliveryList.data.trip_info.team_name || \"General Team\"\n    };\n    TripReports.generateDeliveryList(getDeliveryList.data.items || [], tripInfo);\n  }\n}"
  }
]